<!-- Start Breadcrumb -->
<% @current_bread_name = t('income_categories.income_categories') %>
<!-- End Breadcrumb -->

<div class="row row_frameh">
  <div class="offset-1 col-lg-10">
    <div class="panel panel-border">
      <div class="row">
        <div class="col-lg-12">
          <div class="panel-heading">
            <h1 class="panel-title text-center">Categorias das Receitas</h1>
              <div class="btn-group pull-right">
                <%= link_to new_income_category_path, class: 'btn btn-success btn-sm waves-effect waves-light' do %>
                  <i class="m-l-5 fa fa-plus"></i>
                  <%= t('income_categories.new') %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
        <div class="row">
          <div class="col-lg-12">
            <div class="panel-body">
              <div class="col-sm-12 col-xs-12 col-md-12 col-lg-12 table-responsive">
                <table id="datatable" class="table table-hover">
                  <thead>
                    <tr>
                      <th>Id</th>
                      <th>Nome</th>
                      <th class="text-right">Ações</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% unless @income_categories.present? %>
                      <tr>
                        <td colspan="4" class="text-center">
                          <h3>Sem Categorias cadastradas.</h3>
                          <%= link_to 'click aqui', new_income_category_path %> para cadastrar uma nova categoria
                        </td>
                      </tr>
                    <% end %>
                    <% @income_categories.each do |income_category| %>
                      <tr>
                        <td>
                          <%= income_category.id %>
                        </td>
                        <td>
                          <%= income_category.name %>
                        </td>
                        <td class="text-right">
                          <%= link_to income_category_path(income_category) do %>
                            <i class="fas fa-eye"></i>
                          <% end %>
                          <%= link_to edit_income_category_path(income_category) do %>
                            <i class="fas fa-edit"></i>
                          <% end %>
                          <%= link_to income_category_path(income_category), method: :delete, data: { confirm: t(:remove_question)} do %>
                            <i class="fas fa-trash-alt"></i>
                          <% end %>
                        </td>
                      </tr>
                    <% end %>
                  </tbody>
                </table>
                <div>
                  <%= will_paginate @income_categories, :previous_label => 'Previous', class: "paginate", :next_label => 'Next' %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  