<!-- Start Breadcrumb -->
<% @bread_links = [link_to(t('currency_kinds.currency_kinds'), currency_kinds_path)] %>
<% @current_bread_name = @currency_kind.name %>
<!-- End Breadcrumb -->
<div class="row row_index">
  <div class="offset-2 col-xs-9">
    <div class="panel-heading">
      <h1 class="panel-title text-center">Estado do Contrato</h1>
    </div>
    <div class="panel-body">
      <div class="col-sm-12 col-xs-12 col-md-12 col-lg-12 table-responsive">
        <form class="form-horizontal">
          <div class="form-group">
            <label class="col-sm-2 control-label"><%= t(:key) %></label>
            <div class="col-sm-10">
              <p class="form-control-static"><%= @currency_kind.key %></p>
            </div>
          </div>
          <div class="form-group">
            <label class="col-sm-2 control-label">Nome:</label>
            <div class="col-sm-10">
              <p class="form-control-static"><%= @currency_kind.name %></p>
            </div>
          </div>
          <div class="form-group">
            <div class="col-sm-offset-2 col-sm-9 m-t-15">
                <%= link_to "Sair", currency_kinds_path, class: "btn btn-white btn-sm waves-effect m-l-5" %>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

