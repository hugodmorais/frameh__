<!-- Start Breadcrumb -->
<% @bread_links = [link_to(t('currency_kinds.currency_kinds'), currency_kinds_url)] %>
<% @current_bread_name = t('currency_kinds.new') %>
<!-- End Breadcrumb -->
<div class="row row_index">
  <div class="offset-2 col-lg-8">
    <div class="panel-heading">
      <h1 class="panel-title text-center"><%= t('currency_kinds.new') %></h1>
    </div>
    <div class="panel-body">
      <%= render 'form', currency_kind: @currency_kind %>
    </div>
  </div>
</div>

  
  