<!-- Start Breadcrumb -->
<% @bread_links = [link_to(t('accounts.accounts'), accounts_url)] %>
<% @current_bread_name = t('accounts.new') %>
<!-- End Breadcrumb -->
<div class="row row_index">
  <div class="offset-2 col-lg-8">
    <div class="panel-heading">
      <h1 class="panel-title text-center"><%= t('accounts.new') %></h1>
    </div>
    <div class="panel-body">
      <%= render 'form', account: @account %>
    </div>
  </div>
</div>

  
  